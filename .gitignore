# Rust build artifacts
/target/

# Compiled binaries
spectre         # Main binary (Go orchestrator + scraper + Rust FFI)
spectre-audit   # Security audit binary
go_scraper      # Scraper binary (only needed for Rust standalone at target/release/spectre)

# Runtime-generated proxy pool files
raw_proxies.json
proxies_dns.json
proxies_non_dns.json
proxies_combined.json
last_chain.json

# Log files
*.log
logs/

# Debug / temporary files
*.txt

# Shared library (built locally)
*.so
